[pytest]
asyncio_mode = auto
markers =
    unit: Pure business-logic tests — no external dependencies
    integration: Tests against real Redis infrastructure
    functional: End-to-end API tests via TestClient
